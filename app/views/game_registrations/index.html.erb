<h1><%= t('.title') %><span class="badge"><%= @game_registrations.count %></span></h1>

<div class="row">
  <div class="col-2">
    <legend class="filter-title"><%= t :search_title %></legend>
    <fieldset class="filter-panel">
      <%= search_form_for @q do |f| %>

        <%= f.search_field :user_first_name_cont, {placeholder: t('activerecord.attributes.game_registration.user')} %>

        <%= f.search_field :team_name_cont, {placeholder: t('activerecord.attributes.game_registration.team')} %>

        <br>
        <br>
        <%= f.submit class: 'btn btn-default btn-search-filter' %>
      <% end %>
    </fieldset>
  </div>
  <div class="col">
    <div class="col-md-3 btn-new-top">
      <% if can? :create, GameRegistration %>
        <%= link_to ('<span class="glyphicon glyphicon-plus" aria-hidden="true"></span> '+t('.new_button')).html_safe, url_for(action: :new), class: "btn btn-default" %>
      <% end %>
    </div>
    <table class="table table-hover table-condensed table-bordered">
      <thead>
        <tr>
    
          <th><%= sort_link(@q, :user, default_order: :asc) %></th>
    
          <th></th>
    
          <th><%= sort_link(@q, :team, default_order: :asc) %></th>
    
          <th class="col-xs-1"></th>
        </tr>
      </thead>

      <tbody>
        <% @game_registrations.each do |game_registration| %>
          <tr>
    
            <td><%= game_registration.user %></td>
    
            <td><%= game_registration.futsal_game %></td>
    
            <td><%= game_registration.team %></td>
    
            <td>
              <%= link_to '<span class="glyphicon glyphicon-eye-open"></span>'.html_safe, game_registration %>
              <% if can? :edit, GameRegistration %>
                <%= link_to '<span class="glyphicon glyphicon-pencil"></span>'.html_safe, edit_game_registration_path(game_registration) %>
              <% end %>
              <% if can? :destroy, GameRegistration %>
                <%= link_to '<span class="glyphicon glyphicon-remove"></span>'.html_safe, game_registration, method: :delete, data: { confirm: 'Are you sure?' } %>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <%= will_paginate @game_registrations %>
  </div>
</div>