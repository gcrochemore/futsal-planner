<div class="row">
  <div class="col" style="width: 200px;">
    <h4>Notes par position</h4>
    <div class="col dashboard-panel-content">
       <%= render :template => 'users/_position_rating_chart', :locals => { :user => @user } %>
    </div>
  </div>
  <div class="col">
    <h4>Buts par passeurs</h4>
    <div class="col dashboard-panel-content">
      <table class="table table-hover table-sm table-bordered">
        <thead>
          <tr>
            <th></th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% @user.goals_by_assist.each do |assist, count| %>
            <tr>
              <td>
                <%= !assist.nil? ? User.find(assist.to_i) : "<i>Pas de passeur</i>" %></td><td><%= count %> Passes
                (<%= @user.game_registrations_with.select{|key, hash| key == assist.to_i }.first.andand[1] || '?' %> m.)
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
  <div class="col">
    <h4>Assists par buteur</h4>
    <div class="col dashboard-panel-content">
        <table class="table table-hover table-sm table-bordered">
          <thead>
            <tr>
              <th></th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            <% @user.assists_by_goals.each do |goal, count| %>
              <tr>
                <td>
                    <%= !goal.nil? ? User.find(goal.to_i) : "<i>Pas de buteur</i>" %></td><td><%= count %> Buts
                    (<%= @user.game_registrations_with.select{|key, hash| key == goal.to_i }.first.andand[1] || '?' %> m.)
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
    </div>
  </div>
  <div class="col">
    <h4>Buts encaissés par buteur</h4>
    <div class="col dashboard-panel-content">
        <table class="table table-hover table-sm table-bordered">
          <thead>
            <tr>
              <th></th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            <% @user.goalkeeper_goals_against_by_goals.each do |goal, count| %>
              <tr>
                <td>
                    <%= !goal.nil? ? User.find(goal.to_i) : "<i>Pas de buteur</i>" %></td><td><%= count %> Buts
                    (<%= @user.game_registrations_against.select{|key, hash| key == goal.to_i }.first.andand[1] || '?' %> m.)
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
     </div>
  </div>
  <div class="col">
    <h4>Matchs communs</h4>
    <div class="col dashboard-panel-content">
        <table class="table table-hover table-sm table-bordered">
          <thead>
            <tr>
              <th></th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            <% @user.game_registrations_common.each do |user, count| %>
              <tr>
                <td><%= !user.nil? ? User.find(user.to_i) : "<i>Pas de joueur</i>" %></td><td><%= count %> Matchs</td>
              </tr>
            <% end %>
          </tbody>
        </table>
    </div>
  </div>
</div>
<div class="row">
  <div class="col">
    <h4>Matchs Avec</h4>
    <div class="col dashboard-panel-content">
        <table class="table table-hover table-sm table-bordered">
          <thead>
            <tr>
              <th></th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            <% @user.game_registrations_with.each do |user, count| %>
              <tr>
                <td><%= !user.nil? ? User.find(user.to_i) : "<i>Pas de joueur</i>" %></td><td><%= count %> Matchs</td>
              </tr>
            <% end %>
          </tbody>
        </table>
    </div>
  </div>
  <div class="col">
    <h4>Matchs Contre</h4>
    <div class="col dashboard-panel-content">
        <table class="table table-hover table-sm table-bordered">
          <thead>
            <tr>
              <th></th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            <% @user.game_registrations_against.each do |user, count| %>
              <tr>
                <td><%= !user.nil? ? User.find(user.to_i) : "<i>Pas de joueur</i>" %></td><td><%= count %> Matchs</td>
              </tr>
            <% end %>
          </tbody>
        </table>
    </div>
  </div>
  <div class="col" style="width: 200px;">
      <h4>Aptitudes physiques</h4>
      <div class="col dashboard-panel-content">
         <%= render :template => 'users/_physical_stats_chart', :locals => { :user => @user } %>
      </div>
  </div>
</div>




